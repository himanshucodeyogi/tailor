<!DOCTYPE html>
<html lang="en">
<%- include('../../partials/head') %>
<body class="bg-gray-50 dark:bg-gray-950 min-h-screen">
  <%- include('../../partials/nav-admin') %>

  <div class="max-w-5xl mx-auto px-4 py-6 sm:py-8">
    <%- include('../../partials/flash') %>

    <div class="flex flex-col sm:flex-row sm:justify-between sm:items-center gap-3 mb-6">
      <div class="flex items-center gap-3">
        <span class="material-icons-round text-accent-500 mi-xl">content_cut</span>
        <h1 class="text-2xl sm:text-3xl font-extrabold text-gray-800 dark:text-white">Tailors</h1>
      </div>
      <a href="/admin/tailors/new" class="btn btn-primary w-full sm:w-auto">
        <span class="material-icons-round mi-sm">person_add</span>Create Tailor
      </a>
    </div>

    <% if (tailors.length === 0) { %>
      <div class="card text-center py-10">
        <span class="material-icons-round text-gray-300 dark:text-gray-600" style="font-size:48px;">content_cut</span>
        <p class="text-gray-400 mt-2">No tailors created yet</p>
        <a href="/admin/tailors/new" class="btn btn-primary btn-sm mt-3">Create First Tailor</a>
      </div>
    <% } else { %>
      <!-- Desktop Table -->
      <div class="hidden sm:block card p-0 overflow-hidden">
        <table class="data-table">
          <thead><tr><th class="pl-6">Name</th><th>Username</th><th>Created</th><th>Actions</th></tr></thead>
          <tbody>
            <% tailors.forEach((tailor) => { %>
              <tr>
                <td class="pl-6">
                  <div class="flex items-center gap-3">
                    <div class="w-9 h-9 bg-accent-50 dark:bg-accent-500/10 rounded-full flex items-center justify-center">
                      <span class="material-icons-round mi-sm text-accent-500">content_cut</span>
                    </div>
                    <span class="font-semibold text-gray-800 dark:text-white"><%= tailor.name %></span>
                  </div>
                </td>
                <td><code class="bg-gray-100 dark:bg-gray-700 px-2 py-1 rounded text-xs"><%= tailor.username %></code></td>
                <td class="text-xs text-gray-500"><%= new Date(tailor.createdAt).toLocaleDateString() %></td>
                <td>
                  <form action="/admin/tailors/<%= tailor._id %>?_method=DELETE" method="POST" style="display:inline;" onsubmit="return confirm('Delete this tailor?');">
                    <button type="submit" class="btn btn-ghost btn-sm" style="color:#D32F2F;">
                      <span class="material-icons-round mi-sm">delete</span>Delete
                    </button>
                  </form>
                </td>
              </tr>
            <% }) %>
          </tbody>
        </table>
      </div>

      <!-- Mobile Cards -->
      <div class="sm:hidden space-y-3">
        <% tailors.forEach((tailor) => { %>
          <div class="card">
            <div class="flex justify-between items-start mb-2">
              <div class="flex items-center gap-3">
                <div class="w-10 h-10 bg-accent-50 dark:bg-accent-500/10 rounded-full flex items-center justify-center">
                  <span class="material-icons-round text-accent-500">content_cut</span>
                </div>
                <div>
                  <p class="font-semibold text-gray-800 dark:text-white"><%= tailor.name %></p>
                  <code class="text-xs text-gray-500"><%= tailor.username %></code>
                </div>
              </div>
              <form action="/admin/tailors/<%= tailor._id %>?_method=DELETE" method="POST" onsubmit="return confirm('Delete?');">
                <button type="submit" class="btn btn-danger btn-sm"><span class="material-icons-round mi-sm">delete</span></button>
              </form>
            </div>
          </div>
        <% }) %>
      </div>
    <% } %>

    <div class="card mt-6 border-l-4 border-l-primary-500">
      <div class="flex items-start gap-3">
        <span class="material-icons-round text-primary-500 mt-0.5">info</span>
        <p class="text-sm text-gray-600 dark:text-gray-400">
          After creating a tailor, share the username and password with them. They can log in at <code class="bg-gray-100 dark:bg-gray-800 px-2 py-1 rounded text-xs">/tailor/login</code> to manage orders.
        </p>
      </div>
    </div>
  </div>

  <%- include('../../partials/footer') %>
</body>
</html>
